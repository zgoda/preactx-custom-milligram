{
  "private": true,
  "name": "preactx-custom-milligram",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-preact": "^1.1.1",
    "identity-obj-proxy": "^3.0.0",
    "node-sass": "^4.14.1",
    "preact-cli": "^3.0.1",
    "preact-render-spy": "^1.3.0",
    "sass-loader": "^10.0.1",
    "sirv-cli": "1.0.6"
  },
  "dependencies": {
    "milligram": "^1.4.1",
    "preact": "^10.4.8",
    "preact-render-to-string": "^5.1.10",
    "preact-router": "^3.2.1"
  }
}
